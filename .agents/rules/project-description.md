---
trigger: manual
---

# Project Description

The current project is a RAG system project based on Python and the langchain + langgraph framework, using a conda virtual environment (the environment name is RAG).

When you need to download Python packages or update package versions, prioritize using conda commands for the operation.